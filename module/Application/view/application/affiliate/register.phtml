<?php

$title = "Register as an Affiliate";
$this->headTitle($title);

$form = $this->registrationform;
$form->setAttribute('action', $this->url('affiliate', array('action' => 'register')));
$form->prepare();
?>
<h1><?php echo $this->escapeHtml($title)?></h1>

<?php echo $this->partial('application/partial/registration', array('form' => $form))?>